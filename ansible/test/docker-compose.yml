#This is a prototype template

version: '3.3'

services:
  osg-core:
    ports:
      - '4000:4000'
    container_name: 'osg-client'
    image: 'filippandza/medical-images-client:latest'
    environment:
      - 'SERVER_PORT=4000' #Must match to second part of ports variable :<some-port-val>